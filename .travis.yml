git:
  depth: false

deploy:
  edge:
    branch: v1.8.47
    provider: pages
    skip-cleanup: true
    github-token: $GITHUB_TOKEN
    keep-history: true
    local-dir: public
    repo: amsimp/amsimp
    target-branch: master
    verbose: true
    on:
      branch: master